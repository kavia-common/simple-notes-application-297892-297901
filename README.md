# simple-notes-application-297892-297901

Simple Notes application.

- Backend (FastAPI) on port 3001 with PostgreSQL via SQLAlchemy.
- Frontend (React) on port 3000.
- Database default connection: postgresql://postgres:postgres@localhost:5001/postgres (can override with env DATABASE_URL).
